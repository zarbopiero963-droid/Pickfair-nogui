name: CloudCLI PR Fix
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "Pull request number to fix"
        required: true
        type: string
      dry_run:
        description: "Inspect only. Do not edit/commit/push."
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"
      force:
        description: "Force run even if guard conditions would normally skip"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      extra_handoff:
        description: "Optional extra handoff text"
        required: false
        type: string

  repository_dispatch:
    types:
      - cloudcli_pr_fix
      - telegram_pr_fix

permissions:
  contents: read
  pull-requests: read
  issues: read
  checks: read
  statuses: read
  actions: read
  security-events: read

concurrency:
  group: cloudcli-pr-fix-${{ github.event.inputs.pr_number || github.event.client_payload.pr_number || github.event.client_payload.pr || github.run_id }}
  cancel-in-progress: false

jobs:
  pr-fix:
    name: Send guarded PR fix to CloudCLI Codex
    runs-on: ubuntu-latest
    timeout-minutes: 120

    env:
      CLOUDCLI_API_KEY: "${{ secrets.CLOUDCLI_API_KEY }}"
      CLOUDCLI_BASE_URL: "${{ vars.CLOUDCLI_BASE_URL || secrets.CLOUDCLI_BASE_URL || 'https://cloudcli.ai/api/v1' }}"
      CLOUDCLI_ENVIRONMENT_ID: "${{ secrets.CLOUDCLI_ENVIRONMENT_ID || vars.CLOUDCLI_ENVIRONMENT_ID }}"
      CLOUDCLI_PROJECT_NAME: "${{ secrets.CLOUDCLI_PROJECT_NAME || vars.CLOUDCLI_PROJECT_NAME }}"
      CLOUDCLI_REPO_PATH: "${{ secrets.CLOUDCLI_REPO_PATH || vars.CLOUDCLI_REPO_PATH }}"
      CLOUDCLI_PROVIDER: "${{ vars.CLOUDCLI_PROVIDER || secrets.CLOUDCLI_PROVIDER || 'codex' }}"
      CLOUDCLI_MODEL: "${{ vars.CLOUDCLI_MODEL || secrets.CLOUDCLI_MODEL || 'gpt-5.2' }}"
      GH_TOKEN_FOR_AGENT: "${{ secrets.PICKFAIR_ACTIONS_TOKEN }}"
      TELEGRAM_BOT_TOKEN: "${{ secrets.TELEGRAM_BOT_TOKEN }}"
      TELEGRAM_CHAT_ID: "${{ secrets.TELEGRAM_CHAT_ID }}"

    steps:
      - name: Build guarded handoff
        id: handoff
        uses: actions/github-script@v7
        with:
          github-token: "${{ secrets.PICKFAIR_ACTIONS_TOKEN }}"
          script: |
            const fs = require("fs");

            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const repository = `${owner}/${repo}`;

            const payload = context.payload.client_payload || {};
            const inputs = context.payload.inputs || {};

            const prNumberRaw = String(inputs.pr_number || payload.pr_number || payload.pr || "").trim();
            const dryRun = String(inputs.dry_run || payload.dry_run || "true").trim() === "true";
            const force = String(inputs.force || payload.force || "false").trim() === "true";
            const extraHandoff = String(inputs.extra_handoff || payload.extra_handoff || payload.handoff_text || "").trim();

            if (!prNumberRaw || !/^\d+$/.test(prNumberRaw)) {
              core.setFailed("Missing or invalid pr_number.");
              return;
            }

            const prNumber = Number(prNumberRaw);

            fs.mkdirSync("cloudcli-output/context", { recursive: true });

            async function paginateSafe(label, fn, args) {
              try {
                return await github.paginate(fn, args);
              } catch (err) {
                core.warning(`${label} failed: ${err.message}`);
                return [
                  {
                    __fetch_error: true,
                    label,
                    message: err.message,
                  },
                ];
              }
            }

            async function graphqlSafe(label, query, variables, fallback) {
              try {
                return await github.graphql(query, variables);
              } catch (err) {
                core.warning(`${label} failed: ${err.message}`);
                return fallback;
              }
            }

            function writeJson(name, value) {
              fs.writeFileSync(
                `cloudcli-output/context/${name}`,
                JSON.stringify(value, null, 2),
                "utf8"
              );
            }

            function clip(value, max = 1600) {
              const text = String(value || "").trim();
              if (text.length <= max) return text;
              return text.slice(0, max) + "\n...[truncated]";
            }

            function authorOf(item) {
              return item.user?.login || item.author?.login || "unknown";
            }

            function shortComment(item) {
              return {
                id: item.id || item.databaseId || null,
                author: authorOf(item),
                path: item.path || null,
                line: item.line || item.original_line || item.originalLine || null,
                created_at: item.created_at || item.createdAt || null,
                updated_at: item.updated_at || item.updatedAt || null,
                url: item.html_url || item.url || null,
                body: clip(item.body || "", 1200),
              };
            }

            function shortCheck(check) {
              return {
                id: check.id,
                name: check.name,
                status: check.status,
                conclusion: check.conclusion,
                details_url: check.details_url,
                html_url: check.html_url,
                output_title: clip(check.output?.title || "", 600),
                output_summary: clip(check.output?.summary || "", 1600),
                output_text: clip(check.output?.text || "", 1600),
              };
            }

            function shortStatus(status) {
              return {
                context: status.context,
                state: status.state,
                description: status.description,
                target_url: status.target_url,
                created_at: status.created_at,
                updated_at: status.updated_at,
              };
            }

            const { data: pr } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number: prNumber,
            });

            if (pr.state !== "open") {
              core.setFailed(`PR #${prNumber} is not open. Current state: ${pr.state}`);
              return;
            }

            const files = await paginateSafe(
              "changed files",
              github.rest.pulls.listFiles,
              {
                owner,
                repo,
                pull_number: prNumber,
                per_page: 100,
              }
            );

            const issueComments = await paginateSafe(
              "issue comments",
              github.rest.issues.listComments,
              {
                owner,
                repo,
                issue_number: prNumber,
                per_page: 100,
              }
            );

            const reviewComments = await paginateSafe(
              "review comments",
              github.rest.pulls.listReviewComments,
              {
                owner,
                repo,
                pull_number: prNumber,
                per_page: 100,
              }
            );

            const reviews = await paginateSafe(
              "reviews",
              github.rest.pulls.listReviews,
              {
                owner,
                repo,
                pull_number: prNumber,
                per_page: 100,
              }
            );

            const checkRunsResponse = await github.rest.checks.listForRef({
              owner,
              repo,
              ref: pr.head.sha,
              per_page: 100,
            });

            const checkRuns = checkRunsResponse.data.check_runs || [];

            const statuses = await paginateSafe(
              "commit statuses",
              github.rest.repos.listCommitStatusesForRef,
              {
                owner,
                repo,
                ref: pr.head.sha,
                per_page: 100,
              }
            );

            const reviewThreadsQuery = `
              query($owner: String!, $repo: String!, $number: Int!, $cursor: String) {
                repository(owner: $owner, name: $repo) {
                  pullRequest(number: $number) {
                    reviewThreads(first: 100, after: $cursor) {
                      pageInfo {
                        hasNextPage
                        endCursor
                      }
                      nodes {
                        id
                        isResolved
                        isOutdated
                        path
                        line
                        originalLine
                        comments(first: 30) {
                          nodes {
                            id
                            databaseId
                            author {
                              login
                            }
                            body
                            createdAt
                            updatedAt
                            url
                            path
                            line
                            originalLine
                            diffHunk
                          }
                        }
                      }
                    }
                  }
                }
              }
            `;

            const reviewThreads = [];
            let cursor = null;

            while (true) {
              const result = await graphqlSafe(
                "review threads graphql",
                reviewThreadsQuery,
                {
                  owner,
                  repo,
                  number: prNumber,
                  cursor,
                },
                {
                  __fetch_error: true,
                  message: "GraphQL review threads failed",
                }
              );

              if (result.__fetch_error) {
                reviewThreads.push(result);
                break;
              }

              const connection = result?.repository?.pullRequest?.reviewThreads;
              if (!connection) {
                break;
              }

              for (const thread of connection.nodes || []) {
                reviewThreads.push({
                  ...thread,
                  comments: thread.comments?.nodes || [],
                });
              }

              if (!connection.pageInfo?.hasNextPage) {
                break;
              }

              cursor = connection.pageInfo.endCursor;
            }

            const failedCheckRuns = checkRuns.filter((check) => {
              const status = String(check.status || "").toLowerCase();
              const conclusion = String(check.conclusion || "").toLowerCase();
              return status === "completed" && !["success", "neutral", "skipped"].includes(conclusion);
            });

            const skippedCheckRuns = checkRuns.filter((check) => {
              return String(check.conclusion || "").toLowerCase() === "skipped";
            });

            const pendingCheckRuns = checkRuns.filter((check) => {
              return String(check.status || "").toLowerCase() !== "completed";
            });

            const latestStatusesByContext = new Map();

            for (const status of statuses.filter((item) => !item.__fetch_error)) {
              const key = status.context || String(status.id) || "unknown";
              if (!latestStatusesByContext.has(key)) {
                latestStatusesByContext.set(key, status);
              }
            }

            const latestStatuses = Array.from(latestStatusesByContext.values());

            const failedOrPendingStatuses = latestStatuses.filter((status) => {
              return String(status.state || "").toLowerCase() !== "success";
            });

            const botCommentSummary = {};

            for (const comment of [...issueComments, ...reviewComments].filter((item) => !item.__fetch_error)) {
              const login = authorOf(comment);
              botCommentSummary[login] = (botCommentSummary[login] || 0) + 1;
            }

            const unresolvedThreads = reviewThreads.filter((thread) => {
              return !thread.__fetch_error && thread.isResolved === false;
            });

            const hasBlockingFailure = failedCheckRuns.length > 0 || failedOrPendingStatuses.length > 0;

            const failedNames = [
              ...failedCheckRuns.map((check) => check.name),
              ...failedOrPendingStatuses.map((status) => status.context),
            ].filter(Boolean);

            const summary = {
              repository,
              prNumber,
              prUrl: pr.html_url,
              title: pr.title,
              baseBranch: pr.base.ref,
              baseSha: pr.base.sha,
              headBranch: pr.head.ref,
              headSha: pr.head.sha,
              dryRun,
              force,
              hasBlockingFailure,
              failedNames,
              counts: {
                changedFiles: files.length,
                issueComments: issueComments.length,
                reviewComments: reviewComments.length,
                reviews: reviews.length,
                reviewThreads: reviewThreads.length,
                unresolvedThreads: unresolvedThreads.length,
                checkRuns: checkRuns.length,
                failedCheckRuns: failedCheckRuns.length,
                skippedCheckRuns: skippedCheckRuns.length,
                pendingCheckRuns: pendingCheckRuns.length,
                statuses: latestStatuses.length,
                failedOrPendingStatuses: failedOrPendingStatuses.length,
              },
              changedFiles: files.map((file) => ({
                filename: file.filename,
                status: file.status,
                additions: file.additions,
                deletions: file.deletions,
                changes: file.changes,
                blob_url: file.blob_url,
                raw_url: file.raw_url,
              })),
              failedCheckRuns: failedCheckRuns.map(shortCheck),
              skippedCheckRuns: skippedCheckRuns.map(shortCheck),
              pendingCheckRuns: pendingCheckRuns.map(shortCheck),
              failedOrPendingStatuses: failedOrPendingStatuses.map(shortStatus),
              botCommentSummary,
              recentIssueComments: issueComments.filter((item) => !item.__fetch_error).slice(-30).map(shortComment),
              recentReviewComments: reviewComments.filter((item) => !item.__fetch_error).slice(-60).map(shortComment),
              unresolvedThreads: unresolvedThreads.slice(0, 100).map((thread) => ({
                id: thread.id,
                isResolved: thread.isResolved,
                isOutdated: thread.isOutdated,
                path: thread.path,
                line: thread.line,
                originalLine: thread.originalLine,
                comments: (thread.comments || []).map(shortComment),
              })),
            };

            writeJson("summary.json", summary);
            writeJson("files.json", files);
            writeJson("issue-comments.json", issueComments);
            writeJson("review-comments.json", reviewComments);
            writeJson("reviews.json", reviews);
            writeJson("review-threads.json", reviewThreads);
            writeJson("check-runs.json", checkRuns);
            writeJson("statuses.json", statuses);

            const handoff = [
              "# CLOUDCLI PR FIX GUARDED HANDOFF",
              "",
              `Repository: ${repository}`,
              `PR: #${prNumber}`,
              `PR URL: ${pr.html_url}`,
              `Title: ${pr.title}`,
              `Base branch: ${pr.base.ref}`,
              `Head branch: ${pr.head.ref}`,
              `Head SHA: ${pr.head.sha}`,
              `Dry run: ${dryRun}`,
              `Force: ${force}`,
              `Has blocking failure: ${hasBlockingFailure}`,
              `Failed names: ${failedNames.join(", ") || "none"}`,
              "",
              "## Context counts",
              "",
              "```json",
              JSON.stringify(summary.counts, null, 2),
              "```",
              "",
              "## Failed checks/statuses",
              "",
              "```json",
              JSON.stringify(
                {
                  failedCheckRuns: summary.failedCheckRuns,
                  failedOrPendingStatuses: summary.failedOrPendingStatuses,
                },
                null,
                2
              ),
              "```",
              "",
              "## Changed files",
              "",
              "```json",
              JSON.stringify(summary.changedFiles, null, 2),
              "```",
              "",
              "## Recent review comments and unresolved threads sample",
              "",
              "```json",
              JSON.stringify(
                {
                  recentReviewComments: summary.recentReviewComments,
                  unresolvedThreads: summary.unresolvedThreads,
                },
                null,
                2
              ),
              "```",
              "",
              "## Extra handoff",
              "",
              extraHandoff || "No extra handoff text provided.",
              "",
              "## Hard rules",
              "",
              "- Work only on the existing PR branch.",
              "- Do not create a new branch.",
              "- Do not create a new PR.",
              "- Do not work on main/master.",
              "- Do not merge.",
              "- Do not use rg. ripgrep is not installed. Use grep, sed, find, or python3.",
              "- Python may be available only as python3.",
              "- Do not run non-existing tests. Discover test files with find/ls first.",
              "- Do not rely only on local pytest if GitHub checks/statuses are red.",
              "- If DeepSource: Python, Codacy, or any required check/status is failing, real mode is not allowed to no-op.",
              "- In real mode with any failing check/status, either commit and push a targeted fix, or output NEEDS_DEEPSOURCE_DETAILS / NEEDS_STATIC_ANALYSIS_DETAILS with Push: failed.",
              "- Never return Files changed: none plus Push: skipped dry-run in real mode while a check/status is failing.",
              "- Never return Commit: none in real mode while a check/status is failing.",
              "- If live external check details are unavailable, infer likely static-analysis fixes from current code and PR comments.",
              "- For this repo, likely static-analysis fixes include D203 blank line before class docstrings and SLF001 noqa only in tests where private access is intentional.",
              "- Run py_compile and targeted existing tests after edits.",
              "",
            ].join("\n");

            fs.writeFileSync("cloudcli-output/handoff-lite.md", handoff, "utf8");

            fs.writeFileSync(
              "cloudcli-output/pr-metadata.json",
              JSON.stringify(
                {
                  owner,
                  repo,
                  repository,
                  prNumber,
                  prUrl: pr.html_url,
                  title: pr.title,
                  baseBranch: pr.base.ref,
                  baseSha: pr.base.sha,
                  headBranch: pr.head.ref,
                  headSha: pr.head.sha,
                  dryRun,
                  force,
                  hasBlockingFailure,
                  failedNames,
                  counts: summary.counts,
                },
                null,
                2
              ),
              "utf8"
            );

            core.setOutput("repository", repository);
            core.setOutput("pr_number", String(prNumber));
            core.setOutput("pr_url", pr.html_url);
            core.setOutput("title", pr.title);
            core.setOutput("base_branch", pr.base.ref);
            core.setOutput("head_branch", pr.head.ref);
            core.setOutput("head_sha", pr.head.sha);
            core.setOutput("dry_run", dryRun ? "true" : "false");
            core.setOutput("force", force ? "true" : "false");
            core.setOutput("failed_checks", String(failedCheckRuns.length));
            core.setOutput("skipped_checks", String(skippedCheckRuns.length));
            core.setOutput("review_threads", String(reviewThreads.length));
            core.setOutput("issue_comments", String(issueComments.length));
            core.setOutput("review_comments", String(reviewComments.length));
            core.setOutput("has_blocking_failure", hasBlockingFailure ? "true" : "false");
            core.setOutput("failed_names", failedNames.join(", "));
            core.setOutput("prompt_mode", "guarded-no-noop");

      - name: Execute CloudCLI PR fix
        id: execute
        run: |
          set -euo pipefail

          python3 <<'PY'
          import json
          import os
          import time
          import urllib.error
          import urllib.request
          from pathlib import Path

          out_dir = Path("cloudcli-output")
          out_dir.mkdir(exist_ok=True)

          api_key = os.environ.get("CLOUDCLI_API_KEY", "").strip()
          base_url = os.environ.get("CLOUDCLI_BASE_URL", "").strip() or "https://cloudcli.ai/api/v1"
          env_id = os.environ.get("CLOUDCLI_ENVIRONMENT_ID", "").strip()
          project = os.environ.get("CLOUDCLI_PROJECT_NAME", "").strip()
          repo_path = os.environ.get("CLOUDCLI_REPO_PATH", "").strip()
          provider = os.environ.get("CLOUDCLI_PROVIDER", "").strip() or "codex"
          model = os.environ.get("CLOUDCLI_MODEL", "").strip() or "gpt-5.2"
          github_token = os.environ.get("GH_TOKEN_FOR_AGENT", "").strip()

          metadata = json.loads(Path("cloudcli-output/pr-metadata.json").read_text(encoding="utf-8"))
          handoff_lite = Path("cloudcli-output/handoff-lite.md").read_text(encoding="utf-8")

          repository = metadata["repository"]
          pr_number = metadata["prNumber"]
          pr_url = metadata["prUrl"]
          title = metadata["title"]
          base_branch = metadata["baseBranch"]
          head_branch = metadata["headBranch"]
          head_sha = metadata["headSha"]
          dry_run = bool(metadata["dryRun"])
          has_blocking_failure = bool(metadata.get("hasBlockingFailure"))
          failed_names = metadata.get("failedNames") or []

          required = {
              "CLOUDCLI_API_KEY": api_key,
              "CLOUDCLI_ENVIRONMENT_ID": env_id,
              "CLOUDCLI_PROJECT_NAME": project,
              "CLOUDCLI_REPO_PATH": repo_path,
              "PICKFAIR_ACTIONS_TOKEN": github_token,
          }

          missing = [name for name, value in required.items() if not value]
          if missing:
              raise SystemExit("Missing required config: " + ", ".join(missing))

          if len(handoff_lite) > 140000:
              handoff_lite = handoff_lite[:140000] + "\n\n...[handoff-lite truncated to avoid CloudCLI payload limit]"

          def request_json(method, path, payload=None):
              url = base_url.rstrip("/") + path
              data = None
              headers = {
                  "X-API-KEY": api_key,
                  "Accept": "application/json",
              }

              if payload is not None:
                  data = json.dumps(payload).encode("utf-8")
                  headers["Content-Type"] = "application/json"

              req = urllib.request.Request(url, data=data, headers=headers, method=method)

              try:
                  with urllib.request.urlopen(req, timeout=120) as resp:
                      raw = resp.read().decode("utf-8", errors="replace")
              except urllib.error.HTTPError as exc:
                  body = exc.read().decode("utf-8", errors="replace")
                  raise SystemExit(f"HTTP {exc.code} {method} {url}\n{body}") from exc

              if not raw.strip():
                  return {}

              return json.loads(raw)

          env_info = request_json("GET", f"/environments/{env_id}")
          status = str(env_info.get("status", ""))

          print(f"CloudCLI environment status: {status}")

          if status != "running":
              print("Starting CloudCLI environment...")
              request_json("POST", f"/environments/{env_id}/start")

              for attempt in range(1, 49):
                  time.sleep(5)
                  env_info = request_json("GET", f"/environments/{env_id}")
                  status = str(env_info.get("status", ""))
                  print(f"Start poll {attempt}: {status}")

                  if status == "running":
                      break

              if status != "running":
                  raise SystemExit("CloudCLI environment did not become running.")

          if dry_run:
              execution_rules = """
          DRY RUN MODE.

          Do not edit files.
          Do not commit.
          Do not push.
          Do not create a PR.

          You must inspect the local PR branch and produce a concrete fix plan.
          If checks/statuses are failing, do not say no work is needed.
          """
          else:
              execution_rules = f"""
          REAL EXECUTION MODE.

          You must:
          - Work only in this repo path: {repo_path}
          - Fetch latest refs.
          - Checkout the existing PR branch: {head_branch}
          - Pull latest origin/{head_branch}.
          - Do NOT create a new branch.
          - Do NOT create a new PR.
          - Do NOT merge.
          - Do NOT work directly on main/master.
          - Fix all still-valid blocking issues.
          - Run relevant checks/tests.
          - Commit only intended changes.
          - Push only to the same PR branch: {head_branch}
          - Prefer: git push origin HEAD:{head_branch}

          Real-mode no-op guard:
          - has_blocking_failure={has_blocking_failure}
          - failed_names={failed_names}
          - If has_blocking_failure is true, you are NOT allowed to finish with Files changed: none.
          - If has_blocking_failure is true, you are NOT allowed to finish with Commit: none.
          - If has_blocking_failure is true, you are NOT allowed to finish with Push: skipped dry-run.
          - If you cannot determine a safe fix, output NEEDS_DEEPSOURCE_DETAILS or NEEDS_STATIC_ANALYSIS_DETAILS and Push: failed.
          """

          message = f"""
          CTO MODE: CLOUDCLI EXISTING PR FIX - GUARDED NO-NOOP MODE

          Repository: {repository}
          Repo path: {repo_path}
          PR: #{pr_number}
          PR URL: {pr_url}
          PR title: {title}
          Base branch: {base_branch}
          Existing PR branch: {head_branch}
          Current PR head SHA before work: {head_sha}

          {execution_rules}

          Non-negotiable rules:
          - Do not use rg. ripgrep is not installed. Use grep, sed, find, or python3.
          - Python may be available only as python3.
          - Discover test file names before running tests.
          - Do not run non-existing tests.
          - Do not rely only on local pytest while GitHub checks/statuses are red.
          - If DeepSource: Python or Codacy is failing, real mode must either push a fix commit or explicitly fail with NEEDS_DEEPSOURCE_DETAILS / NEEDS_STATIC_ANALYSIS_DETAILS.
          - Never report Push: skipped dry-run in real mode.
          - Never report Commit: none in real mode while checks/statuses are failing.
          - Never report Files changed: none in real mode while checks/statuses are failing.
          - End final response with marker CLOUDCLI_PR_FIX_DONE.

          Initial commands:
          - pwd
          - cd "{repo_path}" && git status --short
          - cd "{repo_path}" && git branch --show-current
          - cd "{repo_path}" && git remote -v
          - cd "{repo_path}" && git fetch origin
          - cd "{repo_path}" && git checkout "{head_branch}"
          - cd "{repo_path}" && git pull --ff-only origin "{head_branch}"

          Handoff follows:

          {handoff_lite}

          Return final output only in this format:

          CLOUDCLI_PR_FIX_DONE

          Final factual status:
          - DONE / PARTIAL / NOT DONE / NEEDS_DEEPSOURCE_DETAILS / NEEDS_STATIC_ANALYSIS_DETAILS

          PR:
          - {pr_url}

          Branch:
          - {head_branch}

          Commit:
          - <new commit SHA or none>

          Checks:
          - <exact commands and results>

          Files changed:
          - <files>

          Files created:
          - <files>

          Issues fixed:
          - <items>

          Findings skipped with reason:
          - <items>

          What was not done:
          - <items>

          Push:
          - succeeded / failed / skipped dry-run
          """

          payload = {
              "environmentId": env_id,
              "projectName": project,
              "message": message,
              "provider": provider,
              "model": model,
              "createBranch": False,
              "createPR": False,
              "githubToken": github_token,
          }

          redacted_payload = dict(payload)
          redacted_payload["githubToken"] = "***"
          redacted_payload["message_length"] = len(message)
          redacted_payload["message"] = message[:20000] + (
              "\n...[truncated in redacted log only]" if len(message) > 20000 else ""
          )

          Path("cloudcli-output/agent-payload-redacted.json").write_text(
              json.dumps(redacted_payload, ensure_ascii=False, indent=2),
              encoding="utf-8",
          )

          url = base_url.rstrip("/") + "/agent/execute"

          req = urllib.request.Request(
              url,
              data=json.dumps(payload).encode("utf-8"),
              headers={
                  "X-API-KEY": api_key,
                  "Content-Type": "application/json",
                  "Accept": "text/event-stream",
              },
              method="POST",
          )

          print("Calling CloudCLI agent execute...")
          print(f"Prompt length: {len(message)} characters")

          chunks = []

          try:
              with urllib.request.urlopen(req, timeout=7200) as resp:
                  for raw_line in resp:
                      line = raw_line.decode("utf-8", errors="replace")
                      print(line, end="")
                      chunks.append(line)
          except urllib.error.HTTPError as exc:
              body = exc.read().decode("utf-8", errors="replace")
              raise SystemExit(f"HTTP {exc.code} POST {url}\n{body}") from exc

          sse_text = "".join(chunks)
          Path("cloudcli-output/agent-sse.log").write_text(sse_text, encoding="utf-8")

          plain_parts = []

          for line in sse_text.splitlines():
              line = line.strip()

              if not line.startswith("data:"):
                  continue

              raw = line[5:].strip()

              if not raw:
                  continue

              try:
                  obj = json.loads(raw)
              except Exception:
                  plain_parts.append(raw)
                  continue

              for key in ("content", "output", "message", "text"):
                  value = obj.get(key)
                  if isinstance(value, str) and value.strip():
                      plain_parts.append(value)

              tool_input = obj.get("toolInput")
              if isinstance(tool_input, dict):
                  command = tool_input.get("command")
                  if isinstance(command, str) and command.strip():
                      plain_parts.append("$ " + command)

          plain_text = "\n".join(plain_parts)
          Path("cloudcli-output/agent-text.log").write_text(plain_text, encoding="utf-8")

          combined_lower = (sse_text + "\n" + plain_text).lower()
          plain_lower = plain_text.lower()

          marker_found = "cloudcli_pr_fix_done" in combined_lower

          Path("cloudcli-output/agent-marker-status.txt").write_text(
              "marker_found=true\n" if marker_found else "marker_found=false\n",
              encoding="utf-8",
          )

          if not marker_found:
              if dry_run:
                  print("WARNING: CLOUDCLI_PR_FIX_DONE marker not found in dry-run output.")
              else:
                  raise SystemExit("CloudCLI PR fix marker CLOUDCLI_PR_FIX_DONE not found in agent output.")

          hard_fail_indicators = [
              "needs_manual_update_branch",
              "needs_deepsource_details",
              "needs_static_analysis_details",
              "fatal: could not read username",
              "authentication failed",
              "permission denied",
              "push:\n- failed",
              "push failed",
              "failed to push",
              "could not read from remote repository",
          ]

          if any(indicator in combined_lower for indicator in hard_fail_indicators):
              raise SystemExit("CloudCLI reported push/auth/manual-update/details-needed failure. Failing workflow intentionally.")

          if not dry_run:
              bad_real_outputs = [
                  "push:\n- skipped dry-run",
                  "push:\n- skipped",
                  "files changed:\n- none",
                  "commit:\n- none",
              ]

              if has_blocking_failure and any(indicator in plain_lower for indicator in bad_real_outputs):
                  raise SystemExit("Real mode had blocking failures but CloudCLI produced no commit/push. Failing workflow intentionally.")

              push_success_markers = [
                  "push:\n- succeeded",
                  "push: succeeded",
              ]

              if has_blocking_failure and not any(marker in plain_lower for marker in push_success_markers):
                  raise SystemExit("Real mode had blocking failures but CloudCLI did not clearly report Push: succeeded.")

              if "final factual status:\n- not done" in combined_lower:
                  raise SystemExit("CloudCLI reported NOT DONE. Failing workflow intentionally.")

          print("CloudCLI PR fix execution completed.")
          PY

      - name: Verify PR state after fix
        id: verify
        if: always()
        uses: actions/github-script@v7
        with:
          github-token: "${{ secrets.PICKFAIR_ACTIONS_TOKEN }}"
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = Number("${{ steps.handoff.outputs.pr_number || github.event.inputs.pr_number || github.event.client_payload.pr_number || github.event.client_payload.pr || 0 }}");

            if (!prNumber) {
              core.setOutput("pr_url", "");
              core.setOutput("head_sha", "");
              core.setOutput("head_branch", "");
              core.setOutput("state", "");
              return;
            }

            const { data: pr } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number: prNumber,
            });

            core.setOutput("pr_url", pr.html_url);
            core.setOutput("head_sha", pr.head.sha);
            core.setOutput("head_branch", pr.head.ref);
            core.setOutput("state", pr.state);

      - name: Upload CloudCLI PR fix logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cloudcli-pr-fix-guarded-${{ github.run_id }}
          path: cloudcli-output/**
          if-no-files-found: ignore

      - name: Notify Telegram success
        if: success()
        env:
          PR_NUMBER: "${{ steps.handoff.outputs.pr_number }}"
          PR_URL: "${{ steps.verify.outputs.pr_url || steps.handoff.outputs.pr_url }}"
          BRANCH: "${{ steps.verify.outputs.head_branch || steps.handoff.outputs.head_branch }}"
          HEAD_SHA: "${{ steps.verify.outputs.head_sha || steps.handoff.outputs.head_sha }}"
          DRY_RUN: "${{ steps.handoff.outputs.dry_run }}"
          FAILED_CHECKS: "${{ steps.handoff.outputs.failed_checks }}"
          SKIPPED_CHECKS: "${{ steps.handoff.outputs.skipped_checks }}"
          REVIEW_THREADS: "${{ steps.handoff.outputs.review_threads }}"
          ISSUE_COMMENTS: "${{ steps.handoff.outputs.issue_comments }}"
          REVIEW_COMMENTS: "${{ steps.handoff.outputs.review_comments }}"
          HAS_BLOCKING_FAILURE: "${{ steps.handoff.outputs.has_blocking_failure }}"
          FAILED_NAMES: "${{ steps.handoff.outputs.failed_names }}"
          PROMPT_MODE: "${{ steps.handoff.outputs.prompt_mode }}"
        run: |
          set -euo pipefail

          if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
            echo "Telegram secrets missing. Skipping notification."
            exit 0
          fi

          python3 <<'PY'
          import os
          import urllib.parse
          import urllib.request

          token = os.environ["TELEGRAM_BOT_TOKEN"]
          chat_id = os.environ["TELEGRAM_CHAT_ID"]

          text = "\n".join([
              "CloudCLI PR fix completed",
              "",
              f"PR: #{os.environ.get('PR_NUMBER', '')}",
              f"URL: {os.environ.get('PR_URL', '')}",
              f"Branch: {os.environ.get('BRANCH', '')}",
              f"Head SHA: {os.environ.get('HEAD_SHA', '')}",
              f"Dry run: {os.environ.get('DRY_RUN', '')}",
              f"Prompt mode: {os.environ.get('PROMPT_MODE', '')}",
              f"Blocking failure: {os.environ.get('HAS_BLOCKING_FAILURE', '')}",
              f"Failed names: {os.environ.get('FAILED_NAMES', '')}",
              "",
              "Context collected:",
              f"- Failed checks: {os.environ.get('FAILED_CHECKS', '')}",
              f"- Skipped checks: {os.environ.get('SKIPPED_CHECKS', '')}",
              f"- Review threads: {os.environ.get('REVIEW_THREADS', '')}",
              f"- Issue comments: {os.environ.get('ISSUE_COMMENTS', '')}",
              f"- Review comments: {os.environ.get('REVIEW_COMMENTS', '')}",
              "",
              f"Run: https://github.com/{os.environ.get('GITHUB_REPOSITORY')}/actions/runs/{os.environ.get('GITHUB_RUN_ID')}",
          ])

          data = urllib.parse.urlencode({
              "chat_id": chat_id,
              "text": text,
              "disable_web_page_preview": "true",
          }).encode()

          urllib.request.urlopen(
              f"https://api.telegram.org/bot{token}/sendMessage",
              data=data,
              timeout=60,
          ).read()
          PY

      - name: Notify Telegram failure
        if: failure()
        env:
          PR_NUMBER: "${{ steps.handoff.outputs.pr_number || github.event.inputs.pr_number || github.event.client_payload.pr_number || github.event.client_payload.pr || 'unknown' }}"
          PR_URL: "${{ steps.verify.outputs.pr_url || steps.handoff.outputs.pr_url || '' }}"
          BRANCH: "${{ steps.verify.outputs.head_branch || steps.handoff.outputs.head_branch || '' }}"
          HEAD_SHA: "${{ steps.verify.outputs.head_sha || steps.handoff.outputs.head_sha || '' }}"
          DRY_RUN: "${{ steps.handoff.outputs.dry_run || github.event.inputs.dry_run || github.event.client_payload.dry_run || 'unknown' }}"
          FAILED_CHECKS: "${{ steps.handoff.outputs.failed_checks || 'unknown' }}"
          SKIPPED_CHECKS: "${{ steps.handoff.outputs.skipped_checks || 'unknown' }}"
          REVIEW_THREADS: "${{ steps.handoff.outputs.review_threads || 'unknown' }}"
          ISSUE_COMMENTS: "${{ steps.handoff.outputs.issue_comments || 'unknown' }}"
          REVIEW_COMMENTS: "${{ steps.handoff.outputs.review_comments || 'unknown' }}"
          HAS_BLOCKING_FAILURE: "${{ steps.handoff.outputs.has_blocking_failure || 'unknown' }}"
          FAILED_NAMES: "${{ steps.handoff.outputs.failed_names || 'unknown' }}"
          PROMPT_MODE: "${{ steps.handoff.outputs.prompt_mode || 'unknown' }}"
        run: |
          set -euo pipefail

          if [ -z "${TELEGRAM_BOT_TOKEN:-}" ] || [ -z "${TELEGRAM_CHAT_ID:-}" ]; then
            echo "Telegram secrets missing. Skipping notification."
            exit 0
          fi

          python3 <<'PY'
          import os
          import urllib.parse
          import urllib.request

          token = os.environ["TELEGRAM_BOT_TOKEN"]
          chat_id = os.environ["TELEGRAM_CHAT_ID"]

          text = "\n".join([
              "CloudCLI PR fix failed",
              "",
              f"PR: #{os.environ.get('PR_NUMBER', '')}",
              f"URL: {os.environ.get('PR_URL', '')}",
              f"Branch: {os.environ.get('BRANCH', '')}",
              f"Head SHA: {os.environ.get('HEAD_SHA', '')}",
              f"Dry run: {os.environ.get('DRY_RUN', '')}",
              f"Prompt mode: {os.environ.get('PROMPT_MODE', '')}",
              f"Blocking failure: {os.environ.get('HAS_BLOCKING_FAILURE', '')}",
              f"Failed names: {os.environ.get('FAILED_NAMES', '')}",
              "",
              "Context collected before failure:",
              f"- Failed checks: {os.environ.get('FAILED_CHECKS', '')}",
              f"- Skipped checks: {os.environ.get('SKIPPED_CHECKS', '')}",
              f"- Review threads: {os.environ.get('REVIEW_THREADS', '')}",
              f"- Issue comments: {os.environ.get('ISSUE_COMMENTS', '')}",
              f"- Review comments: {os.environ.get('REVIEW_COMMENTS', '')}",
              "",
              f"Run: https://github.com/{os.environ.get('GITHUB_REPOSITORY')}/actions/runs/{os.environ.get('GITHUB_RUN_ID')}",
              "",
              "Open artifact cloudcli-pr-fix-guarded-* for logs and context bundle.",
          ])

          data = urllib.parse.urlencode({
              "chat_id": chat_id,
              "text": text,
              "disable_web_page_preview": "true",
          }).encode()

          urllib.request.urlopen(
              f"https://api.telegram.org/bot{token}/sendMessage",
              data=data,
              timeout=60,
          ).read()
          PY