name: money-management

on:
  push:
    paths:
      - "core/money_management.py"
      - "tests/integration/test_money_management*"
  pull_request:
    paths:
      - "core/money_management.py"

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      exists: ${{ steps.chk.outputs.exists }}
    steps:
      - uses: actions/checkout@v4
      - id: chk
        run: |
          [ -f "core/money_management.py" ] && echo "exists=true" >> $GITHUB_OUTPUT || echo "exists=false" >> $GITHUB_OUTPUT

  run:
    needs: check
    if: needs.check.outputs.exists == 'true'
    uses: ./.github/workflows/_module-ultra-check.yml
    with:
      module_path: "core/money_management.py"
      test_paths: "tests/integration/test_money_management*"