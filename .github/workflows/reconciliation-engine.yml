name: reconciliation-engine

on:
  push:
    paths:
      - "core/reconciliation_engine.py"
      - "tests/reconciliation/*"
  pull_request:
    paths:
      - "core/reconciliation_engine.py"

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      exists: ${{ steps.chk.outputs.exists }}
    steps:
      - uses: actions/checkout@v4
      - id: chk
        run: |
          [ -f "core/reconciliation_engine.py" ] && echo "exists=true" >> $GITHUB_OUTPUT || echo "exists=false" >> $GITHUB_OUTPUT

  run:
    needs: check
    if: needs.check.outputs.exists == 'true'
    uses: ./.github/workflows/_module-ultra-check.yml
    with:
      module_path: "core/reconciliation_engine.py"
      test_paths: "tests/reconciliation"