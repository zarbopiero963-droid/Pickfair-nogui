name: observability-tests

on:
  push:
    paths:
      - "observability/**"
      - "tests/observability/**"
      - "diagnostics/**"
      - "services/**"
      - "storage/**"
      - ".github/workflows/observability-tests.yml"
  pull_request:
    paths:
      - "observability/**"
      - "tests/observability/**"
      - "diagnostics/**"
      - "services/**"
      - "storage/**"
      - ".github/workflows/observability-tests.yml"
  workflow_dispatch:

jobs:
  observability:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-observability-${{ hashFiles('requirements.txt', 'requirements-dev.txt', 'pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-observability-
            ${{ runner.os }}-pip-

      - name: Upgrade pip
        run: |
          python -m pip install --upgrade pip setuptools wheel

      - name: Install project dependencies
        run: |
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
          if [ -f pyproject.toml ]; then pip install .; fi
          pip install pytest

      - name: Show Python environment
        run: |
          python --version
          pip --version
          pytest --version

      - name: Run observability tests
        run: |
          pytest tests/observability -q -x

      - name: Run individual observability test files
        run: |
          pytest tests/observability/test_health_registry.py -q
          pytest tests/observability/test_metrics_registry.py -q
          pytest tests/observability/test_alerts_manager.py -q
          pytest tests/observability/test_diagnostics_service.py -q
