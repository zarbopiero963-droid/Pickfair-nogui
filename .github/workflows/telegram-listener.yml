name: telegram-listener

on:
  push:
    paths:
      - "telegram_listener.py"
      - "tests/integration/test_telegram_listener*"
  pull_request:
    paths:
      - "telegram_listener.py"

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      exists: ${{ steps.chk.outputs.exists }}
    steps:
      - uses: actions/checkout@v4
      - id: chk
        run: |
          [ -f "telegram_listener.py" ] && echo "exists=true" >> $GITHUB_OUTPUT || echo "exists=false" >> $GITHUB_OUTPUT

  run:
    needs: check
    if: needs.check.outputs.exists == 'true'
    uses: ./.github/workflows/_module-ultra-check.yml
    with:
      module_path: "telegram_listener.py"
      test_paths: "tests/integration/test_telegram_listener*"